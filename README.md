# 桃園市租金檢核系統（React + TypeScript + Next.js 靜態）

## 本機啟動
```bash
npm i
npm run dev
```

## 產出靜態網站（可部署到 Cloudflare Pages / GitHub Pages）
```bash
npm run build
# 靜態輸出在 out/
```

## Cloudflare Pages（建議）
- Framework preset: Next.js
- Build command: `npm run build`
- Output directory: `out`
